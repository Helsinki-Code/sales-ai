export function LeadsTable({ leads = [] }: { leads?: any[] }) { return <div className="ui-card"><table className="data-table"><thead><tr><th>Grade</th><th>Company</th><th>Contact</th><th>Email</th><th>Status</th><th>Created</th></tr></thead><tbody>{leads.map((lead)=><tr key={lead.id}><td>{lead.grade} {lead.score}</td><td>{lead.company_name}</td><td>{lead.contact_name}<br/><span className="muted small">{lead.contact_title}</span></td><td>{lead.contact_email}<br/><span className="muted small">{lead.email_confidence}</span></td><td>{lead.status}</td><td>{lead.created_at ? new Date(lead.created_at).toLocaleDateString() : ""}</td></tr>)}</tbody></table>{leads.length===0?<p className="muted">No saved leads yet. Run Lead Finder first.</p>:null}</div>; }
