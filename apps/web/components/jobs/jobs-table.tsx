export function JobsTable({ jobs = [] }: { jobs?: any[] }) { return <div className="ui-card"><table className="data-table"><thead><tr><th>Status</th><th>Endpoint</th><th>Stage</th><th>Progress</th><th>Created</th></tr></thead><tbody>{jobs.map((job)=><tr key={job.id}><td>{job.status}</td><td>{job.endpoint}</td><td>{job.stage}</td><td>{job.progress}%</td><td>{job.created_at ? new Date(job.created_at).toLocaleString() : ""}</td></tr>)}</tbody></table>{jobs.length===0?<p className="muted">No jobs yet.</p>:null}</div>; }
