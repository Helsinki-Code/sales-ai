import { Progress } from "@/components/ui/progress";
const stages = ["queued","target_strategy","company_discovery","scoring","evidence_packaging","persisting_result","complete"];
export function JobTimeline({ job }: { job?: any }) { const stage = job?.stage ?? job?.status ?? "queued"; const progress = Number(job?.progress ?? (stage === "complete" ? 100 : 10)); return <div className="ui-card timeline"><div><strong>Job timeline</strong><p className="muted small">{job?.message ?? stage}</p><Progress value={progress} /></div>{stages.map((item) => <div className="timeline-item" key={item}><span className="timeline-dot" /><div><strong>{item.replaceAll("_", " ")}</strong><p className="muted small">{item === stage ? "Current stage" : "Lead finder workflow stage"}</p></div></div>)}</div>; }
