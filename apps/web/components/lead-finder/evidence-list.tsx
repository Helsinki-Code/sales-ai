export function EvidenceList({ evidence = [] }: { evidence?: any[] }) { return <div className="detail-panel">{evidence.length === 0 ? <p className="muted small">No evidence captured.</p> : evidence.map((item, i) => <a key={i} href={item.url} target="_blank" rel="noreferrer" className="ui-card"><strong>{item.title ?? item.url}</strong><p className="muted small">{item.quote}</p></a>)}</div>; }
