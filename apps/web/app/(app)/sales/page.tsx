import Link from "next/link";
import { TOOL_INFO } from "@/lib/sales/endpoints";
export default function SalesPage() { const tools = Object.entries(TOOL_INFO).filter(([endpoint]) => endpoint !== "leads"); return <main className="app-grid"><div className="ui-card"><p className="eyebrow">AI Sales Tools</p><h1 className="page-title">Secondary tools</h1><p className="muted">Lead Finder is now the flagship workflow.</p><Link href="/lead-finder" className="ui-button ui-button-primary">Open Lead Finder</Link></div><div className="metrics-grid">{tools.map(([endpoint, tool]) => <Link key={endpoint} href={`/sales/${endpoint}`} className="ui-card"><h3>{tool.name}</h3><p className="muted small">{tool.description}</p><span className="ui-badge ui-badge-accent">{tool.type}</span></Link>)}</div></main>; }
